apiVersion: v1
kind: PersistentVolume
metadata:
  name: pv-source
spec:
  storageClassName: "default"
  accessModes:
    - ReadWriteOnce
  capacity:
    storage: 1Gi
  hostPath:
    path: /data/vl-0001/
    type: DirectoryOrCreate
